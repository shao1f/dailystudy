package main

func MoveZeor(nums []int) {
	for i:=0;i<len(nums);i++ {
		if nums[i] == 0 {
			for j:=i+1;j<len(nums);j++ {
				if
			}
		}
	}
}

func main() {
	// buff := make([]byte, 16)
	// n, err := rand.Read(buff)
	// if n != 0 && err == nil {
	// 	uuid := fmt.Sprintf("%x-%x-%x-%x-%x", buff[0:4], buff[4:6], buff[6:8], buff[8:10], buff[10:])
	// 	fmt.Println(uuid)
	// }
}
